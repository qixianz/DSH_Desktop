# DeepSeek Harness Build - Python dependencies for the window launcher exe.
# Installed into Build\.venv by 00_env.bat (pip install -r requirements.txt).
# Pinned to the versions the packaging pipeline was verified with.
# Pure ASCII on purpose: keep it readable under any code page.
pywebview==6.2.1
pyinstaller==6.22.0
